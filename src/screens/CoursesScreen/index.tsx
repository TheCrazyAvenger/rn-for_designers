import React from 'react';
import {View} from 'react-native';

import {H5, H6} from '../../ui/Typography';
import {styles} from './styles';

export const CoursesScreen: React.FC = () => {
  return (
    <View style={styles.container}>
      <H5>Courses Screen</H5>
    </View>
  );
};
